@import '../../variables'

.block-heading
        flexRowJustify(space-between)
        width 100%
        line-height 20px
        font-family Arial, sans-serif
        font-size 18px
        font-weight bold
        color $activeText