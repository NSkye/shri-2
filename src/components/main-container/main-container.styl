@import '../../variables'

.main-container
    flexColumnJustify(space-between)
    box-sizing border-box
    height 100vh
    width 100%
    padding-top 92px
    @media(max-width $breakpoint)
        height auto