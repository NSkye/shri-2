mixin navigation(items)
    nav.navigation
        ul.navigation__list
            each item in items
                li.navigation__item
                    a.navigation__link(href=item.href)
                        span= item.name