@import '../../variables'

.row-wrapper
    flexRow()
    flex-wrap no-wrap
    margin 0
    padding 0
    width 100vw
    height auto
    @media(max-width $breakpoint)
        flex-direction column
        align-items center